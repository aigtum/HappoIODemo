version: 2.1
orbs:
  happo: happo/happo@2.0.0
jobs:
  build:
    docker:
      - image: circleci/node:jessie-browsers
    steps:
      - checkout
      - run:
          name: happo
          command: npm run happo-ci-circleci